import React from "react";
import "./CustomizeProperties.scss";

const CustomizeProperties = () => {
    return <div className="properties-block-section"></div>;
};

export default CustomizeProperties;
